interface:
  display_name: "ORBCAFE UI Router"
  short_description: "Route ORBCAFE requests to the current public control contracts"
  default_prompt: "Use $orbcafe-ui-component-usage first to choose the right ORBCAFE module skill, map natural-language UI requests to canonical public APIs such as NavigationIsland, CValueHelp, CPlanningGantt, AgentPanel, AIBrowserGlow, and CTreeComp, enforce the official Next.js examples baseline, then generate minimal runnable code with public imports only."
