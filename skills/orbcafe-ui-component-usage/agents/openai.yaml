interface:
  display_name: "ORBCAFE UI Router"
  short_description: "Route requests to ORBCAFE skills with the Next examples baseline"
  default_prompt: "Use $orbcafe-ui-component-usage first to choose the right ORBCAFE module skill, enforce the official Next.js examples baseline, then generate minimal runnable code with public APIs only."
