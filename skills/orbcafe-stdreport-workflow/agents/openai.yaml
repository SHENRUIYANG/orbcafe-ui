interface:
  display_name: "ORBCAFE StdReport Workflow"
  short_description: "Build report/list pages with filters, value help, and persistence"
  default_prompt: "Use $orbcafe-stdreport-workflow to implement this report/list page with the official Next.js examples baseline, CStandardPage/CTable, CSmartFilter/CValueHelp where needed, and correct persistence + pagination behavior."
